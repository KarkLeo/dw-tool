import styled from 'styled-components'

export const H2Simple = styled.h2`
  margin: 0;

  font-family: minion-pro-subhead, serif;
  font-weight: 600;
  font-style: normal;
  font-size: 1.6rem;
  text-transform: capitalize;
`

export const PSimple = styled.p`
  margin: 0;

  font-family: minion-pro-caption, serif;
  font-weight: 400;
  font-style: normal;
  font-size: 1.2rem;
  text-align: center;
`
